# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — every variant; the example is the one that belongs in git
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# On-device pose runtime — ~20MB of WASM copied out of node_modules plus
# a 5.5MB model downloaded at build time. Staged by
# scripts/fetch-pose-assets.mjs (npm run pose:assets), never committed.
/public/pose/
